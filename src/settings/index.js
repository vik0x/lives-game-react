export const initCols = 3;
export const initRows = 3;
// speed is an integer that will be multiplied by 10 and then substracted from 1000
// that means the min speed will be one second between ages
export const initSpeed = 0;